---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: podclique-validating-webhook
  {{- /* Only include annotations in manual cert mode (certProvisionMode=manual) */ -}}
  {{- if and (eq .Values.config.server.webhooks.certProvisionMode "manual") .Values.webhooks.podCliqueValidationWebhook.annotations }}
  annotations:
    {{- toYaml .Values.webhooks.podCliqueValidationWebhook.annotations | nindent 4 }}
  {{- end }}
  labels:
{{- include "operator.pclq.validating.webhook.labels" . | nindent 4 }}
webhooks:
  - admissionReviewVersions:
      - v1
    clientConfig:
      {{- if .Values.webhooks.caBundle }}
      caBundle: {{ .Values.webhooks.caBundle }}
      {{- end }}
      service:
        name: {{ required ".Values.service.name is required" .Values.service.name }}
        namespace: {{ .Release.Namespace }}
        path: /webhooks/validate-podclique
        port: {{ required ".Values.config.server.webhooks.port" .Values.config.server.webhooks.port }}
    failurePolicy: Fail
    matchPolicy: Exact
    name: pclq.validating.webhooks.grove.io
    namespaceSelector: { }
    rules:
      - apiGroups:
          - "grove.io"
        apiVersions:
          - "v1alpha1"
        operations:
          - UPDATE
        resources:
          - podcliques
        scope: "Namespaced"
    sideEffects: None
    timeoutSeconds: 10
